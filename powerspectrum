#########POWER_SPECTRUM#########
function [frec,Power] = power_spectrum(y_signal,fs,time)

    t = -time:1/fs:time;
    L = length(t);
    n = 2^nextpow2(L);
    Y = fft(y_signal,n);
    f = fs*(0:(n/2))/n;
    P = abs(Y/n);
    frec = f;
    Power = P(1:n/2+1);
end
